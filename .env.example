# 数据库路径（可选，默认 data/etf_assistant.db）
DATABASE_PATH=data/etf_assistant.db

# 配置文件路径（可选，默认 config.yaml）
CONFIG_PATH=config.yaml

# 行情数据源：auto | akshare | mock
PRICE_DATA_SOURCE=auto

# 后续 AI 模块预留
# OPENAI_API_KEY=
